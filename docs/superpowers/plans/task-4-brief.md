### Task 4: Audit Phần API (docs/05-api)

**Files:**
- Modify: `docs/Drift_Report_VI.md`

**Interfaces:**
- Consumes: `docs/05-api/`, `app/backend/`

- [ ] **Step 1: Audit Phần API**
Đọc các file trong `docs/05-api/`. So sánh spec API trong docs với định nghĩa routes/endpoints thực tế của FastAPI trong `app/backend/`. Tập trung tìm các endpoint mới được code thêm nhưng doc chưa có, hoặc endpoint đã thay đổi signature (đầu vào/ra).

- [ ] **Step 2: Cập nhật Drift_Report_VI.md (Section 3)**
Thay thế dòng `(Chưa có dữ liệu)` trong mục "3. Lệch Pha về API" bằng kết quả phân tích rõ ràng.
