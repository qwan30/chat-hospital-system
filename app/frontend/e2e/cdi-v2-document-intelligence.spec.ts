import { test } from "@playwright/test";

test("upload, correct, approve, explore, chat, and open exact evidence", async ({ page }) => {
  test.fixme(
    true,
    "CDI v2 journey helpers are still placeholders and no matching E2E route exists yet.",
  );
  void page;
});
